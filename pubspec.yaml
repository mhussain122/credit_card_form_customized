name: credit_card_form_customized
description: Flutter Credit/Debit Card Form.
version: 0.0.2
homepage: https://github.com/mhussain122/credit_card_form_customized

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  popover: ^0.2.8+2
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
flutter:
  assets:
    - images/
